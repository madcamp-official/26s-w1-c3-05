# 프론트엔드 환경변수 예시
# 실제 값은 .env (로컬) 또는 배포 플랫폼의 환경변수로 설정한다.

# 백엔드 API 베이스 URL (프로토콜 포함, 끝에 슬래시 없이).
# 미설정 시 개발용 http://localhost:4000 으로 fallback 된다.
# 배포 시에는 HTTPS 백엔드 주소로 반드시 지정할 것 (mixed-content 차단 방지).
VITE_API_BASE_URL=https://api.example.com
